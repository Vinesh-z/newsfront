import { MymodalDirective } from './mymodal.directive';

describe('MymodalDirective', () => {
  it('should create an instance', () => {
    const directive = new MymodalDirective();
    expect(directive).toBeTruthy();
  });
});
